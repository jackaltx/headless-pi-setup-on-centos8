---

# automate ssh 
pi_image_setup_ssh: true

# setup bluetooth
pi_image_setup_bt: false

# setup wlan
pi_image_setup_wlan: false

# automate ssh 
pi_image_setup_cgroup: true

# set to 64 bit mode
pi_image_setup_64bit: true


# image stuff

pi_image_url: https://downloads.raspberrypi.org
pi_image_dir:  /tmp/pi_os
pi_image_name: raspios_lite_arm64_latest

pi_image_boot_mount: /tmp/pi_image_boot
pi_image_rootfs_mount:  /tmp/pi_image_rootfs
